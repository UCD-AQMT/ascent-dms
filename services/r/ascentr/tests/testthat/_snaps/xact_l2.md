# an unrecognised flag in the QC file stops with an informative error

    Code
      xact_l2_from_files(l1b_path, qc_path)
    Condition
      Error in `xact_l2_from_files()`:
      ! Unexpected flag value(s): ZZZ

