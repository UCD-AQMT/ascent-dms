# hour with fewer than 30 valid scans returns NULL with warning (no valid hours)

    Code
      ae33_l2_from_files(l1b_path, qc_path)
    Condition
      Warning in `ae33_l2_from_files()`:
      No valid hours for <l1b_file>
      returning null.
    Output
      NULL

# all invalid scans (qc_outcome=4) warn and return NULL

    Code
      ae33_l2_from_files(l1b_path, qc_path)
    Condition
      Warning in `ae33_l2_from_files()`:
      No valid data in time period!
      Warning in `ae33_l2_from_files()`:
      No valid hours for <l1b_file>
      returning null.
    Output
      NULL

# start_datetime mid-hour leaves too few scans and returns NULL

    Code
      ae33_l2_from_files(l1b_path, qc_path, start_datetime = mid_hour)
    Condition
      Warning in `ae33_l2_from_files()`:
      No valid hours for <l1b_file>
      returning null.
    Output
      NULL

